#include "main.h"

/**
 * print_alphabet - print all the alphabet in lowercase
 * Return: 0
 */
void print_alphabet(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++)
		_putchar(letter);
	_putchar('\n');
}
